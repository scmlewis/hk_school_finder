Phase 1: The "Skeleton" (Data & Map Foundation)Goal: Get the EDB API data onto a functional, mobile-friendly map.Prompt for AI Agent:"Initialize a Vite + React + TypeScript project with Tailwind CSS. Install lucide-react, framer-motion, and leaflet (or maplibre-gl).Data Fetching: Create a service to fetch the EDB 'School Location and Information' API (JSON format).Map Setup: Render a full-screen map centered on Hong Kong.Markers: Map the EDB data to markers. Filter by 'School Level' (Kindergarten, Primary, Secondary).Standardization: Ensure all coordinates are handled as standard WGS84 Lat/Lng. If the API returns HK1980 Grid, include a utility function to convert them.Basic UI: Add a search bar at the top to filter by school name and a 'Locate Me' button."Phase 2: The "Vibe" & Commute Logic (The Use Case C)Goal: Transform it from a map into a tool that solves the "Family Logistics" pain point.Prompt for AI Agent:"Now, let’s add the 'Commute-First' utility and the 'Glassmorphism' UI.MTR Integration: Import a static JSON or fetch the MTR Station/Exit coordinates API.Commute Logic: When a school marker is clicked, calculate the walking distance to the 3 nearest MTR exits.The Bottom Sheet: Create a sliding 'Bottom Sheet' using framer-motion. When a school is selected, slide up to show:School Name (Bilingual).Financing Type (e.g., Aided, DSS).Commute Score: Display the nearest MTR station and estimated walking time.Styling: Use a clean, modern HK-style UI. Rounded corners, subtle blurs (backdrop-filter), and high-contrast typography for readability while walking."Phase 3: The "Deep Dive" (School Net Geometry)Goal: Add the "Strategic Parent" (Use Case A) features once the app is stable.Prompt for AI Agent:"The core app is working. Now we add the School Net Geometry.GeoJSON Layer: Load the Hong Kong Primary School Net boundary GeoJSON files.Spatial Logic: When a user clicks anywhere on the map (or their current location), identify which 'School Net' polygon that point falls into.Visual Feedback: Highlight the active School Net boundary with a subtle stroke and a translucent fill.Filtering: Add a toggle to 'Only show schools within this Net'.PWA Finalization: Configure vite-plugin-pwa so the app is installable and caches the school list for offline use in the MTR."Summary of the Technical StackCategoryRecommended ToolFrameworkReact + TypeScript (Vite)StylingTailwind CSS (for rapid "vibe" adjustments)Map LibraryMapLibre GL (Faster for vector layers/polygons than Leaflet)State ManagementZustand (Lightweight for filtering 1,000+ schools)DeploymentVercel (Instant preview for your mobile testing)